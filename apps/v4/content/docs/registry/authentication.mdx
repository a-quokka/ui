---
title: 인증
description: 인증으로 비공개·맞춤 컴포넌트를 보호합니다.
---

인증을 붙이면 비공개 레지스트리를 운영하고, 누가 컴포넌트에 닿을 수 있는지 정하고, 팀이나 사용자마다 다른 내용을 내보낼 수 있습니다. 흔히 쓰는 인증 방식과 설정 방법을 살펴봅니다.

인증으로 이런 것들이 가능해집니다.

- **비공개 컴포넌트**: 업무 로직과 내부 컴포넌트를 안전하게 지킵니다
- **팀별 자원**: 팀마다 다른 컴포넌트를 내보냅니다
- **접근 제어**: 민감하거나 실험 중인 컴포넌트를 볼 수 있는 사람을 제한합니다
- **사용 분석**: 조직 안에서 누가 어떤 컴포넌트를 쓰는지 봅니다
- **라이선스**: 유료나 라이선스가 걸린 컴포넌트를 누가 받을지 정합니다

## Common Authentication Patterns

### Token-Based Authentication

가장 흔한 방식은 Bearer 토큰이나 API 키입니다.

```json title="components.json"
{
  "registries": {
    "@private": {
      "url": "https://registry.company.com/{name}.json",
      "headers": {
        "Authorization": "Bearer ${REGISTRY_TOKEN}"
      }
    }
  }
}
```

토큰은 환경 변수에 둡니다.

```bash title=".env.local"
REGISTRY_TOKEN=your_secret_token_here
```

### API Key Authentication

헤더에 API 키를 담는 레지스트리도 있습니다.

```json title="components.json"
{
  "registries": {
    "@company": {
      "url": "https://api.company.com/registry/{name}.json",
      "headers": {
        "X-API-Key": "${API_KEY}",
        "X-Workspace-Id": "${WORKSPACE_ID}"
      }
    }
  }
}
```

### Query Parameter Authentication

더 단순하게 하려면 쿼리 파라미터를 씁니다.

```json title="components.json"
{
  "registries": {
    "@internal": {
      "url": "https://registry.company.com/{name}.json",
      "params": {
        "token": "${ACCESS_TOKEN}"
      }
    }
  }
}
```

그러면 이런 주소가 됩니다: `https://registry.company.com/button.json?token=your_token`

## Server-Side Implementation

레지스트리 서버에 인증을 붙이는 방법입니다.

### Next.js API Route Example

```typescript title="app/api/registry/[name]/route.ts"
import { NextRequest, NextResponse } from "next/server"

export async function GET(
  request: NextRequest,
  { params }: { params: { name: string } }
) {
  // Get token from Authorization header.
  const authHeader = request.headers.get("authorization")
  const token = authHeader?.replace("Bearer ", "")

  // Or from query parameters.
  const queryToken = request.nextUrl.searchParams.get("token")

  // Check if token is valid.
  if (!isValidToken(token || queryToken)) {
    return NextResponse.json({ error: "Unauthorized" }, { status: 401 })
  }

  // Check if token can access this component.
  if (!hasAccessToComponent(token, params.name)) {
    return NextResponse.json({ error: "Forbidden" }, { status: 403 })
  }

  // Return the component.
  const component = await getComponent(params.name)
  return NextResponse.json(component)
}

function isValidToken(token: string | null) {
  // Add your token validation logic here.
  // Check against database, JWT validation, etc.
  return token === process.env.VALID_TOKEN
}

function hasAccessToComponent(token: string, componentName: string) {
  // Add role-based access control here.
  // Check if token can access specific component.
  return true // Your logic here.
}
```

### Express.js Example

```javascript title="server.js"
app.get("/registry/:name.json", (req, res) => {
  const token = req.headers.authorization?.replace("Bearer ", "")

  if (!isValidToken(token)) {
    return res.status(401).json({ error: "Unauthorized" })
  }

  const component = getComponent(req.params.name)
  if (!component) {
    return res.status(404).json({ error: "Component not found" })
  }

  res.json(component)
})
```

## Advanced Authentication Patterns

### Team-Based Access

팀마다 다른 컴포넌트를 내보냅니다.

```typescript title="api/registry/route.ts"
async function GET(request: NextRequest) {
  const token = extractToken(request)
  const team = await getTeamFromToken(token)

  // Get components for this team.
  const components = await getComponentsForTeam(team)
  return NextResponse.json(components)
}
```

### User-Personalized Registries

사용자의 설정에 따라 다른 컴포넌트를 내보냅니다.

```typescript
async function GET(request: NextRequest) {
  const user = await authenticateUser(request)

  // Get user's style and framework preferences.
  const preferences = await getUserPreferences(user.id)

  // Get personalized component version.
  const component = await getPersonalizedComponent(params.name, preferences)

  return NextResponse.json(component)
}
```

### Temporary Access Tokens

보안을 위해 만료되는 토큰을 씁니다.

```typescript
interface TemporaryToken {
  token: string
  expiresAt: Date
  scope: string[]
}

async function validateTemporaryToken(token: string) {
  const tokenData = await getTokenData(token)

  if (!tokenData) return false
  if (new Date() > tokenData.expiresAt) return false

  return true
}
```

## Multi-Registry Authentication

[네임스페이스 레지스트리](/docs/registry/namespace)를 쓰면 인증 방식이 서로 다른 레지스트리를 여러 개 둘 수 있습니다.

```json title="components.json"
{
  "registries": {
    "@public": "https://public.company.com/{name}.json",
    "@internal": {
      "url": "https://internal.company.com/{name}.json",
      "headers": {
        "Authorization": "Bearer ${INTERNAL_TOKEN}"
      }
    },
    "@premium": {
      "url": "https://premium.company.com/{name}.json",
      "headers": {
        "X-License-Key": "${LICENSE_KEY}"
      }
    }
  }
}
```

그러면 이런 것이 가능합니다.

- 공개 레지스트리와 비공개 레지스트리를 섞어 씁니다
- 레지스트리마다 다른 인증을 씁니다
- 접근 수준에 따라 컴포넌트를 나눕니다

## Security Best Practices

### Use Environment Variables

토큰을 버전 관리에 커밋하지 마세요. 반드시 환경 변수를 쓰세요.

```bash title=".env.local"
REGISTRY_TOKEN=your_secret_token_here
API_KEY=your_api_key_here
```

그다음 `components.json` 에서 참조합니다.

```json
{
  "registries": {
    "@private": {
      "url": "https://registry.company.com/{name}.json",
      "headers": {
        "Authorization": "Bearer ${REGISTRY_TOKEN}"
      }
    }
  }
}
```

### Use HTTPS

전송 중 토큰이 노출되지 않도록 레지스트리 주소에는 항상 HTTPS 를 쓰세요.

```json
{
  "@secure": "https://registry.company.com/{name}.json" // ✅
  "@insecure": "http://registry.company.com/{name}.json" // ❌
}
```

### Add Rate Limiting

레지스트리가 남용되지 않도록 보호합니다.

```typescript
import rateLimit from "express-rate-limit"

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 minutes
  max: 100, // limit each IP to 100 requests per windowMs
})

app.use("/registry", limiter)
```

### Rotate Tokens

접근 토큰은 주기적으로 바꿉니다.

```typescript
// Create new token with expiration.
function generateToken() {
  const token = crypto.randomBytes(32).toString("hex")
  const expiresAt = new Date(Date.now() + 30 * 24 * 60 * 60 * 1000) // 30 days.

  return { token, expiresAt }
}
```

### Log Access

보안과 분석을 위해 레지스트리 접근을 기록합니다.

```typescript
async function logAccess(request: Request, component: string, userId: string) {
  await db.accessLog.create({
    timestamp: new Date(),
    userId,
    component,
    ip: request.ip,
    userAgent: request.headers["user-agent"],
  })
}
```

## Testing Authentication

인증이 걸린 레지스트리를 로컬에서 확인합니다.

```bash
# Test with curl.
curl -H "Authorization: Bearer your_token" \
  https://registry.company.com/button.json

# Test with the CLI.
REGISTRY_TOKEN=your_token npx shadcn@latest add @private/button
```

## Error Handling

shadcn CLI 는 인증 오류를 이렇게 다룹니다.

- **401 Unauthorized**: 토큰이 없거나 잘못됐습니다
- **403 Forbidden**: 토큰에 이 자원에 대한 권한이 없습니다
- **429 Too Many Requests**: 요청 한도를 넘었습니다

### Custom Error Messages

레지스트리 서버가 응답 본문에 직접 만든 오류 메시지를 담으면 CLI 가 그대로 보여 줍니다.

```typescript
// Registry server returns custom error
return NextResponse.json(
  {
    error: "Unauthorized",
    message:
      "Your subscription has expired. Please renew at company.com/billing",
  },
  { status: 403 }
)
```

사용자에게는 이렇게 보입니다.

```txt
Your subscription has expired. Please renew at company.com/billing
```

상황에 맞는 안내를 줄 수 있습니다.

```typescript
// Different error messages for different scenarios
if (!token) {
  return NextResponse.json(
    {
      error: "Unauthorized",
      message:
        "Authentication required. Set REGISTRY_TOKEN in your .env.local file",
    },
    { status: 401 }
  )
}

if (isExpiredToken(token)) {
  return NextResponse.json(
    {
      error: "Unauthorized",
      message: "Token expired. Request a new token at company.com/tokens",
    },
    { status: 401 }
  )
}

if (!hasTeamAccess(token, component)) {
  return NextResponse.json(
    {
      error: "Forbidden",
      message: `Component '${component}' is restricted to the Design team`,
    },
    { status: 403 }
  )
}
```

## Next Steps

여러 레지스트리에 인증을 걸거나 더 복잡한 구성이 필요하다면 [네임스페이스 레지스트리](/docs/registry/namespace) 문서를 보세요. 이런 내용을 다룹니다.

- 인증이 걸린 레지스트리를 여러 개 설정하기
- 네임스페이스마다 다른 인증 쓰기
- 레지스트리를 넘나드는 의존성 해석
- 더 복잡한 인증 구성
