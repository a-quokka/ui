---
title: Resizable
description: 영역 경계를 끌어 크기를 조절합니다. 키보드로도 됩니다.
base: base
component: true
links:
  doc: https://github.com/bvaughn/react-resizable-panels
  api: https://github.com/bvaughn/react-resizable-panels/tree/main/packages/react-resizable-panels
---

<ComponentPreview
  styleName="base-nova"
  name="resizable-demo"
  previewClassName="h-80"
/>

## About

`Resizable`은 [bvaughn](https://github.com/bvaughn)이 만든 [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels) 위에 만들었습니다.

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add resizable
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install react-resizable-panels
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="resizable"
  title="components/ui/resizable.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  ResizableHandle,
  ResizablePanel,
  ResizablePanelGroup,
} from "@/components/ui/resizable"
```

```tsx showLineNumbers
<ResizablePanelGroup orientation="horizontal">
  <ResizablePanel>One</ResizablePanel>
  <ResizableHandle />
  <ResizablePanel>Two</ResizablePanel>
</ResizablePanelGroup>
```

## Composition

아래 구조로 `ResizablePanelGroup`을 만듭니다.

```text
ResizablePanelGroup
├── ResizablePanel
├── ResizableHandle
└── ResizablePanel
```

## Vertical

세로로 크기를 조절하려면 `orientation="vertical"`을 씁니다.

<ComponentPreview styleName="base-nova" name="resizable-vertical" />

## Handle

`ResizableHandle`에 `withHandle` prop을 주면 손잡이가 눈에 보입니다.

<ComponentPreview styleName="base-nova" name="resizable-handle" />

## i18n

언어를 바꾸면 컴포넌트 안의 문구가 함께 바뀝니다. 오른쪽에서 왼쪽으로 읽는 언어를 지원하려면 [RTL 설정 가이드](/docs/rtl)를 보세요.

<ComponentPreview styleName="base-nova" name="resizable-rtl" direction="rtl" />

## API Reference

자세한 내용은 [react-resizable-panels](https://github.com/bvaughn/react-resizable-panels/tree/main/packages/react-resizable-panels) 문서를 보세요.

## Changelog

### 2025-02-02 `react-resizable-panels` v4

`react-resizable-panels` v4로 올렸습니다. 자세한 내용은 [v4.0.0 릴리스 노트](https://github.com/bvaughn/react-resizable-panels/releases/tag/4.0.0)를 보세요.

`react-resizable-panels` 프리미티브를 직접 쓰고 있다면 아래가 달라졌습니다.

| v3                           | v4                      |
| ---------------------------- | ----------------------- |
| `PanelGroup`                 | `Group`                 |
| `PanelResizeHandle`          | `Separator`             |
| `direction` prop             | `orientation` prop      |
| `defaultSize={50}`           | `defaultSize="50%"`     |
| `onLayout`                   | `onLayoutChange`        |
| `ImperativePanelHandle`      | `PanelImperativeHandle` |
| `ref` prop on Panel          | `panelRef` prop         |
| `data-panel-group-direction` | `aria-orientation`      |

<Callout>
  Dropshot UI 래퍼
  컴포넌트(`ResizablePanelGroup`·`ResizablePanel`·`ResizableHandle`)는
  그대로입니다.
</Callout>
