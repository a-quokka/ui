---
title: Calendar
description: 날짜를 고릅니다. 기간 선택도 됩니다.
base: base
component: true
links:
  doc: https://react-day-picker.js.org
---

<ComponentPreview
  styleName="base-nova"
  name="calendar-demo"
  previewClassName="h-96"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add calendar
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install react-day-picker date-fns
```

<Step>프로젝트에 `Button` 컴포넌트를 추가합니다.</Step>

`Calendar`는 `Button` 컴포넌트를 씁니다. 프로젝트에 설치돼 있는지 확인하세요.

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="calendar"
  title="components/ui/calendar.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { Calendar } from "@/components/ui/calendar"
```

```tsx showLineNumbers
const [date, setDate] = React.useState<Date | undefined>(new Date())

return (
  <Calendar
    mode="single"
    selected={date}
    onSelect={setDate}
    className="rounded-lg border"
  />
)
```

자세한 내용은 [React DayPicker](https://react-day-picker.js.org) 문서를 보세요.

## About

`Calendar`는 [React DayPicker](https://react-day-picker.js.org) 위에 만들었습니다.

## Date Picker

`<Calendar>`로 날짜 선택기를 만들 수 있습니다. 자세한 내용은 [Date Picker](/docs/components/base/date-picker)를 보세요.

## Persian / Hijri / Jalali Calendar

페르시아력을 쓰려면 `components/ui/calendar.tsx`에서 `react-day-picker`를 `react-day-picker/persian`으로 바꾸세요.

```diff
- import { DayPicker } from "react-day-picker"
+ import { DayPicker } from "react-day-picker/persian"
```

<ComponentPreview
  styleName="base-nova"
  name="calendar-hijri"
  title="Persian / Hijri / Jalali Calendar"
  description="A Persian calendar."
  previewClassName="h-[400px]"
/>

## Selected Date (With TimeZone)

Calendar는 `timeZone` prop을 받아 사용자의 현지 시간대로 날짜를 보여 주고 선택하게 합니다.

```tsx showLineNumbers
export function CalendarWithTimezone() {
  const [date, setDate] = React.useState<Date | undefined>(undefined)
  const [timeZone, setTimeZone] = React.useState<string | undefined>(undefined)

  React.useEffect(() => {
    setTimeZone(Intl.DateTimeFormat().resolvedOptions().timeZone)
  }, [])

  return (
    <Calendar
      mode="single"
      selected={date}
      onSelect={setDate}
      timeZone={timeZone}
    />
  )
}
```

**참고:** 20일을 골랐는데 19일이 표시되는 식으로 날짜가 하루씩 밀린다면 `timeZone` prop이 사용자의 현지 시간대로 설정돼 있는지 확인하세요.

**왜 클라이언트에서 처리하나요?** 시간대는 `useEffect` 안에서 `Intl.DateTimeFormat().resolvedOptions().timeZone`으로 알아냅니다. 서버 사이드 렌더링과 맞추기 위해서입니다. 렌더링 중에 시간대를 알아내면 서버와 클라이언트의 시간대가 달라 하이드레이션이 어긋납니다.

## Basic

기본 캘린더입니다. `className="rounded-lg border"`로 꾸몄습니다.

<ComponentPreview
  styleName="base-nova"
  name="calendar-basic"
  previewClassName="h-96"
/>

## Range Calendar

`mode="range"` prop으로 기간 선택을 켭니다.

<ComponentPreview
  styleName="base-nova"
  name="calendar-range"
  previewClassName="h-[36rem] md:h-96"
/>

## Month and Year Selector

`captionLayout="dropdown"`으로 월·연도 드롭다운을 보여 줍니다.

<ComponentPreview
  styleName="base-nova"
  name="calendar-caption"
  previewClassName="h-96"
/>

## Presets

<ComponentPreview
  styleName="base-nova"
  name="calendar-presets"
  previewClassName="h-[650px]"
/>

## Date and Time Picker

<ComponentPreview
  styleName="base-nova"
  name="calendar-time"
  previewClassName="h-[600px]"
/>

## Booked dates

<ComponentPreview
  styleName="base-nova"
  name="calendar-booked-dates"
  previewClassName="h-96"
/>

## Custom Cell Size

<ComponentPreview
  styleName="base-nova"
  name="calendar-custom-days"
  title="Custom Cell Size"
  description="A calendar with custom cell size that's responsive."
  className="**:[.preview]:h-[560px]"
/>

`--cell-size` CSS 변수로 캘린더 칸 크기를 바꿉니다. 브레이크포인트별 값을 주면 반응형으로도 만들 수 있습니다.

```tsx showLineNumbers
<Calendar
  mode="single"
  selected={date}
  onSelect={setDate}
  className="rounded-lg border [--cell-size:--spacing(11)] md:[--cell-size:--spacing(12)]"
/>
```

고정 값을 쓸 수도 있습니다.

```tsx showLineNumbers
<Calendar
  mode="single"
  selected={date}
  onSelect={setDate}
  className="rounded-lg border [--cell-size:2.75rem] md:[--cell-size:3rem]"
/>
```

## Week Numbers

`showWeekNumber`로 주차를 표시합니다.

<ComponentPreview
  styleName="base-nova"
  name="calendar-week-numbers"
  previewClassName="h-96"
/>

## RTL

shadcn/ui에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

페르시아력·히즈라력·잘랄리력을 켜는 방법은 [Hijri Guide](#persian--hijri--jalali-calendar)도 함께 보세요.

<ComponentPreview
  styleName="base-nova"
  name="calendar-rtl"
  direction="rtl"
  previewClassName="h-96"
/>

RTL을 쓸 때는 `react-day-picker/locale`에서 로케일을 가져와 Calendar에 `locale`과 `dir` prop을 함께 넘기세요.

```tsx showLineNumbers
import { arSA } from "react-day-picker/locale"

;<Calendar
  mode="single"
  selected={date}
  onSelect={setDate}
  locale={arSA}
  dir="rtl"
/>
```

## API Reference

`Calendar`에 대한 자세한 내용은 [React DayPicker](https://react-day-picker.js.org) 문서를 보세요.

## Changelog

### RTL Support

이전 버전 `Calendar`를 쓰고 있다면 로케일을 지원하기 위해 아래를 적용해야 합니다.

<Steps>

<Step>`Locale` 타입을 import 합니다.</Step>

`react-day-picker`에서 가져오는 목록에 `Locale`을 추가합니다.

```diff
  import {
    DayPicker,
    getDefaultClassNames,
    type DayButton,
+   type Locale,
  } from "react-day-picker"
```

<Step>Calendar 컴포넌트에 `locale` prop을 추가합니다.</Step>

컴포넌트 props에 `locale`을 추가합니다.

```diff
  function Calendar({
    className,
    classNames,
    showOutsideDays = true,
    captionLayout = "label",
    buttonVariant = "ghost",
+   locale,
    formatters,
    components,
    ...props
  }: React.ComponentProps<typeof DayPicker> & {
    buttonVariant?: React.ComponentProps<typeof Button>["variant"]
  }) {
```

<Step>DayPicker에 `locale`을 넘깁니다.</Step>

`DayPicker` 컴포넌트에 `locale` prop을 넘깁니다.

```diff
    <DayPicker
      showOutsideDays={showOutsideDays}
      className={cn(...)}
      captionLayout={captionLayout}
+     locale={locale}
      formatters={{
        formatMonthDropdown: (date) =>
-         date.toLocaleString("default", { month: "short" }),
+         date.toLocaleString(locale?.code, { month: "short" }),
        ...formatters,
      }}
```

<Step>CalendarDayButton이 locale을 받도록 고칩니다.</Step>

`CalendarDayButton`의 시그니처를 고치고 `locale`을 넘깁니다.

```diff
  function CalendarDayButton({
    className,
    day,
    modifiers,
+   locale,
    ...props
- }: React.ComponentProps<typeof DayButton>) {
+ }: React.ComponentProps<typeof DayButton> & { locale?: Partial<Locale> }) {
```

<Step>CalendarDayButton의 날짜 표기를 고칩니다.</Step>

날짜 표기에 `locale?.code`를 씁니다.

```diff
    <Button
      variant="ghost"
      size="icon"
-     data-day={day.date.toLocaleDateString()}
+     data-day={day.date.toLocaleDateString(locale?.code)}
      ...
    />
```

<Step>DayButton 컴포넌트에 locale을 넘깁니다.</Step>

`DayButton`을 쓰는 곳에서 `locale` prop을 넘기도록 고칩니다.

```diff
      components={{
        ...
-       DayButton: CalendarDayButton,
+       DayButton: ({ ...props }) => (
+         <CalendarDayButton locale={locale} {...props} />
+       ),
        ...
      }}
```

<Step>RTL을 고려한 CSS 클래스로 고칩니다.</Step>

방향이 박힌 클래스를 논리 속성으로 바꾸면 RTL에서 더 잘 맞습니다.

```diff
  // In the day classNames:
- [&:last-child[data-selected=true]_button]:rounded-r-(--cell-radius)
+ [&:last-child[data-selected=true]_button]:rounded-e-(--cell-radius)
- [&:nth-child(2)[data-selected=true]_button]:rounded-l-(--cell-radius)
+ [&:nth-child(2)[data-selected=true]_button]:rounded-s-(--cell-radius)
- [&:first-child[data-selected=true]_button]:rounded-l-(--cell-radius)
+ [&:first-child[data-selected=true]_button]:rounded-s-(--cell-radius)

  // In range_start classNames:
- rounded-l-(--cell-radius) ... after:right-0
+ rounded-s-(--cell-radius) ... after:end-0

  // In range_end classNames:
- rounded-r-(--cell-radius) ... after:left-0
+ rounded-e-(--cell-radius) ... after:start-0

  // In CalendarDayButton className:
- data-[range-end=true]:rounded-r-(--cell-radius)
+ data-[range-end=true]:rounded-e-(--cell-radius)
- data-[range-start=true]:rounded-l-(--cell-radius)
+ data-[range-start=true]:rounded-s-(--cell-radius)
```

</Steps>

여기까지 적용하면 `locale` prop으로 로케일별 표기를 지정할 수 있습니다.

```tsx
import { enUS } from "react-day-picker/locale"

;<Calendar mode="single" selected={date} onSelect={setDate} locale={enUS} />
```
