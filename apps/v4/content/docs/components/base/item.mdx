---
title: Item
description: 이미지·제목·설명·동작을 한 줄에 담는 범용 항목입니다.
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="item-demo" />

`Item`은 거의 모든 내용을 담을 수 있는 단순한 flex 컨테이너입니다. 제목·설명·동작 버튼을 함께 보여 줄 때 씁니다. `ItemGroup`과 묶으면 목록이 됩니다.

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add item
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="item"
  title="components/ui/item.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  Item,
  ItemActions,
  ItemContent,
  ItemDescription,
  ItemMedia,
  ItemTitle,
} from "@/components/ui/item"
```

```tsx showLineNumbers
<Item>
  <ItemMedia variant="icon">
    <Icon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Title</ItemTitle>
    <ItemDescription>Description</ItemDescription>
  </ItemContent>
  <ItemActions>
    <Button>Action</Button>
  </ItemActions>
</Item>
```

## Composition

아래 구조로 `Item`을 만듭니다.

```text
ItemGroup
└── Item
    ├── ItemHeader
    ├── ItemMedia
    ├── ItemContent
    │   ├── ItemTitle
    │   └── ItemDescription
    ├── ItemActions
    └── ItemFooter
```

## Item vs Field

체크박스·입력·라디오·셀렉트처럼 폼 입력을 보여 줘야 한다면 `Field`를 쓰세요.

제목·설명·동작 버튼처럼 내용만 보여 준다면 `Item`을 쓰세요.

## Variant

`variant` prop으로 아이템의 시각 스타일을 바꿉니다.

<ComponentPreview
  styleName="base-nova"
  name="item-variant"
  previewClassName="h-96"
/>

## Size

`size` prop으로 아이템 크기를 바꿉니다. `default`·`sm`·`xs` 중에서 고릅니다.

<ComponentPreview
  styleName="base-nova"
  name="item-size"
  previewClassName="h-96"
/>

## Icon

아이콘을 보여 주려면 `ItemMedia`에 `variant="icon"`을 줍니다.

<ComponentPreview styleName="base-nova" name="item-icon" />

## Avatar

아바타를 보여 주려면 `ItemMedia`에 `variant="avatar"`를 줍니다.

<ComponentPreview styleName="base-nova" name="item-avatar" />

## Image

이미지를 보여 주려면 `ItemMedia`에 `variant="image"`를 줍니다.

<ComponentPreview styleName="base-nova" name="item-image" />

## Group

관련된 아이템은 `ItemGroup`으로 묶습니다.

<ComponentPreview
  styleName="base-nova"
  name="item-group"
  previewClassName="h-96"
/>

## Header

`ItemHeader`로 아이템 내용 위에 헤더를 둡니다.

<ComponentPreview
  styleName="base-nova"
  name="item-header"
  previewClassName="h-96"
/>

## Link

`render` prop으로 아이템을 링크로 그립니다. hover와 focus 상태가 anchor 요소에 적용됩니다.

<ComponentPreview styleName="base-nova" name="item-link" />

```tsx showLineNumbers
<Item render={<a href="/dashboard" />}>
  <ItemMedia variant="icon">
    <HomeIcon />
  </ItemMedia>
  <ItemContent>
    <ItemTitle>Dashboard</ItemTitle>
    <ItemDescription>계정과 활동을 한눈에 봅니다.</ItemDescription>
  </ItemContent>
</Item>
```

## Dropdown

<ComponentPreview styleName="base-nova" name="item-dropdown" />

## RTL

Dropshot UI에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview styleName="base-nova" name="item-rtl" direction="rtl" />

## API Reference

### Item

미디어·제목·설명·동작 버튼을 함께 보여 주는 기본 컴포넌트입니다.

| Prop      | Type                                | Default     |
| --------- | ----------------------------------- | ----------- |
| `variant` | `"default" \| "outline" \| "muted"` | `"default"` |
| `size`    | `"default" \| "sm" \| "xs"`         | `"default"` |
| `render`  | `React.ReactElement`                |             |

### ItemGroup

관련된 아이템을 같은 스타일로 묶는 컨테이너입니다.

```tsx
<ItemGroup>
  <Item />
  <Item />
</ItemGroup>
```

### ItemSeparator

그룹 안 아이템 사이의 구분선입니다.

```tsx
<ItemGroup>
  <Item />
  <ItemSeparator />
  <Item />
</ItemGroup>
```

### ItemMedia

`ItemMedia`로 아이콘·이미지·아바타 같은 미디어를 보여 줍니다.

| Prop      | Type                             | Default     |
| --------- | -------------------------------- | ----------- |
| `variant` | `"default" \| "icon" \| "image"` | `"default"` |

```tsx
<ItemMedia variant="icon">
  <Icon />
</ItemMedia>
```

```tsx
<ItemMedia variant="image">
  <img src="..." alt="..." />
</ItemMedia>
```

### ItemContent

아이템의 제목과 설명을 감쌉니다.

```tsx
<ItemContent>
  <ItemTitle>Title</ItemTitle>
  <ItemDescription>Description</ItemDescription>
</ItemContent>
```

### ItemTitle

아이템의 제목을 보여 줍니다.

```tsx
<ItemTitle>항목 제목</ItemTitle>
```

### ItemDescription

아이템의 설명을 보여 줍니다.

```tsx
<ItemDescription>항목 설명</ItemDescription>
```

### ItemActions

동작 버튼이나 다른 상호작용 요소를 담는 컨테이너입니다.

```tsx
<ItemActions>
  <Button>Action</Button>
</ItemActions>
```

### ItemHeader

아이템 내용 위에 헤더를 보여 줍니다.

```tsx
<Item>
  <ItemHeader>Header</ItemHeader>
  <ItemContent>...</ItemContent>
</Item>
```

### ItemFooter

아이템 내용 아래에 푸터를 보여 줍니다.

```tsx
<Item>
  <ItemContent>...</ItemContent>
  <ItemFooter>Footer</ItemFooter>
</Item>
```
