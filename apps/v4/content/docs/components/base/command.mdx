---
title: Command
description: 검색으로 명령을 찾아 실행합니다.
base: base
component: true
links:
  doc: https://github.com/dip/cmdk
---

<ComponentPreview
  styleName="base-nova"
  name="command-demo"
  align="start"
  previewClassName="h-[24.5rem]"
/>

## About

`<Command />`는 [Dip](https://www.dip.org/)이 만든 [`cmdk`](https://github.com/dip/cmdk) 컴포넌트를 씁니다.

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add command
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install cmdk
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="command"
  title="components/ui/command.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "@/components/ui/command"
```

```tsx showLineNumbers
<Command className="max-w-sm rounded-lg border">
  <CommandInput placeholder="명령을 입력하거나 검색하세요..." />
  <CommandList>
    <CommandEmpty>결과가 없습니다.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>Calendar</CommandItem>
      <CommandItem>이모지 검색</CommandItem>
      <CommandItem>Calculator</CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>Profile</CommandItem>
      <CommandItem>Billing</CommandItem>
      <CommandItem>Settings</CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

## Composition

아래 구조로 `Command`를 만듭니다.

```text
Command
├── CommandInput
└── CommandList
    ├── CommandEmpty
    ├── CommandGroup
    │   ├── CommandItem
    │   └── CommandItem
    ├── CommandSeparator
    └── CommandGroup
        ├── CommandItem
        └── CommandItem
```

## Basic

다이얼로그에 담은 단순한 명령 메뉴입니다.

<ComponentPreview styleName="base-nova" name="command-basic" />

## Shortcuts

<ComponentPreview styleName="base-nova" name="command-shortcuts" />

## Groups

그룹·아이콘·구분선이 있는 명령 메뉴입니다.

<ComponentPreview styleName="base-nova" name="command-groups" />

## Scrollable

항목이 많아 스크롤되는 명령 메뉴입니다.

<ComponentPreview styleName="base-nova" name="command-scrollable" />

## i18n

언어를 바꾸면 컴포넌트 안의 문구가 함께 바뀝니다. 오른쪽에서 왼쪽으로 읽는 언어를 지원하려면 [RTL 설정 가이드](/docs/rtl)를 보세요.

<ComponentPreview
  styleName="base-nova"
  name="command-rtl"
  direction="rtl"
  align="start"
  previewClassName="h-[24.5rem]"
/>

## API Reference

자세한 내용은 [cmdk](https://github.com/dip/cmdk) 문서를 보세요.
