---
title: Date Picker
description: 달력을 열어 날짜를 고릅니다. 기간과 미리 정한 구간을 함께 씁니다.
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="date-picker-demo" />

## Installation

Date Picker는 `<Popover />`와 `<Calendar />`를 조합해 만듭니다.

설치 방법은 [Popover](/docs/components/base/popover#installation)와 [Calendar](/docs/components/base/calendar#installation) 문서를 보세요.

## Usage

```tsx showLineNumbers title="components/example-date-picker.tsx"
"use client"

import * as React from "react"
import { format } from "date-fns"
import { Calendar as CalendarIcon } from "lucide-react"

import { cn } from "@/lib/utils"
import { Button } from "@/components/ui/button"
import { Calendar } from "@/components/ui/calendar"
import {
  Popover,
  PopoverContent,
  PopoverTrigger,
} from "@/components/ui/popover"

export function DatePickerDemo() {
  const [date, setDate] = React.useState<Date>()

  return (
    <Popover>
      <PopoverTrigger
        render={
          <Button
            variant="outline"
            data-empty={!date}
            className="justify-start text-left font-normal data-[empty=true]:text-muted-foreground"
          />
        }
      >
        <CalendarIcon />
        {date ? format(date, "PPP") : <span>Pick a date</span>}
      </PopoverTrigger>
      <PopoverContent className="w-auto p-0">
        <Calendar mode="single" selected={date} onSelect={setDate} />
      </PopoverContent>
    </Popover>
  )
}
```

자세한 내용은 [React DayPicker](https://react-day-picker.js.org) 문서를 보세요.

## Composition

날짜 선택기는 `Popover`와 `Calendar`로 만듭니다. `DatePicker` 라는 루트 컴포넌트는 없습니다.

```text
Popover
├── PopoverTrigger
└── PopoverContent
    └── Calendar
```

## Basic

기본 날짜 선택기입니다.

<ComponentPreview styleName="base-nova" name="date-picker-basic" />

## Range Picker

기간을 고르는 날짜 선택기입니다.

<ComponentPreview styleName="base-nova" name="date-picker-range" />

## Date of Birth

생년월일을 고르는 날짜 선택기입니다. 연·월을 드롭다운으로 고르는 캡션 배치가 들어 있습니다.

<ComponentPreview styleName="base-nova" name="date-picker-dob" />

## Input

입력 칸이 함께 있는 날짜 선택기입니다.

<ComponentPreview styleName="base-nova" name="date-picker-input" />

## Time Picker

시간 입력 칸이 함께 있는 날짜 선택기입니다.

<ComponentPreview styleName="base-nova" name="date-picker-time" />

## Natural Language Picker

`chrono-node` 라이브러리로 자연어 날짜를 해석하는 예제입니다.

<ComponentPreview styleName="base-nova" name="date-picker-natural-language" />

## RTL

Dropshot UI에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview
  styleName="base-nova"
  name="date-picker-rtl"
  direction="rtl"
/>
