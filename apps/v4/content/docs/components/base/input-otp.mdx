---
title: Input OTP
description: 인증번호를 한 자씩 입력합니다. 붙여넣기도 됩니다.
base: base
component: true
links:
  doc: https://input-otp.rodz.dev
---

<ComponentPreview styleName="base-nova" name="input-otp-demo" />

## About

Input OTP는 [@guilherme_rodz](https://twitter.com/guilherme_rodz)가 만든 [input-otp](https://github.com/guilhermerodz/input-otp) 위에 만들었습니다.

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input-otp
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install input-otp
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="input-otp"
  title="components/ui/input-otp.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  InputOTP,
  InputOTPGroup,
  InputOTPSeparator,
  InputOTPSlot,
} from "@/components/ui/input-otp"
```

```tsx showLineNumbers
<InputOTP maxLength={6}>
  <InputOTPGroup>
    <InputOTPSlot index={0} />
    <InputOTPSlot index={1} />
    <InputOTPSlot index={2} />
  </InputOTPGroup>
  <InputOTPSeparator />
  <InputOTPGroup>
    <InputOTPSlot index={3} />
    <InputOTPSlot index={4} />
    <InputOTPSlot index={5} />
  </InputOTPGroup>
</InputOTP>
```

## Composition

아래 구조로 `InputOTP`를 만듭니다.

```text
InputOTP
├── InputOTPGroup
│   ├── InputOTPSlot
│   ├── InputOTPSlot
│   └── InputOTPSlot
├── InputOTPSeparator
├── InputOTPGroup
│   ├── InputOTPSlot
│   ├── InputOTPSlot
│   └── InputOTPSlot
├── InputOTPSeparator
└── InputOTPGroup
    ├── InputOTPSlot
    └── InputOTPSlot
```

## Pattern

`pattern` prop으로 OTP 입력에 원하는 패턴을 지정합니다.

```tsx showLineNumbers {1,5}
import { REGEXP_ONLY_DIGITS_AND_CHARS } from "input-otp"

;<InputOTP maxLength={6} pattern={REGEXP_ONLY_DIGITS_AND_CHARS}>
  ...
</InputOTP>
```

<ComponentPreview styleName="base-nova" name="input-otp-pattern" />

## Separator

`<InputOTPSeparator />`로 입력 그룹 사이에 구분 기호를 넣습니다.

<ComponentPreview styleName="base-nova" name="input-otp-separator" />

## Disabled

`disabled` prop으로 입력을 비활성화합니다.

<ComponentPreview styleName="base-nova" name="input-otp-disabled" />

## Controlled

`value`와 `onChange` prop으로 입력 값을 제어합니다.

<ComponentPreview styleName="base-nova" name="input-otp-controlled" />

## Invalid

슬롯에 `aria-invalid`를 주면 오류 상태로 표시됩니다.

<ComponentPreview styleName="base-nova" name="input-otp-invalid" />

## Four Digits

PIN 번호에 흔히 쓰는 형태입니다. `pattern={REGEXP_ONLY_DIGITS}` prop을 씁니다.

<ComponentPreview styleName="base-nova" name="input-otp-four-digits" />

## Alphanumeric

글자와 숫자를 함께 받으려면 `REGEXP_ONLY_DIGITS_AND_CHARS`를 씁니다.

<ComponentPreview styleName="base-nova" name="input-otp-alphanumeric" />

## Form

<ComponentPreview
  styleName="base-nova"
  name="input-otp-form"
  previewClassName="h-[30rem]"
/>

## i18n

언어를 바꾸면 컴포넌트 안의 문구가 함께 바뀝니다. 오른쪽에서 왼쪽으로 읽는 언어를 지원하려면 [RTL 설정 가이드](/docs/rtl)를 보세요.

<ComponentPreview styleName="base-nova" name="input-otp-rtl" direction="rtl" />

## API Reference

자세한 내용은 [input-otp](https://input-otp.rodz.dev) 문서를 보세요.
