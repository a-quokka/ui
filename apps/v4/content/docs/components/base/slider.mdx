---
title: Slider
description: 정해진 범위 안에서 값을 끌어 고릅니다.
base: base
component: true
links:
  doc: https://base-ui.com/react/components/slider
  api: https://base-ui.com/react/components/slider#api-reference
---

<ComponentPreview styleName="base-nova" name="slider-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add slider
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install @base-ui/react
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="slider"
  title="components/ui/slider.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Slider } from "@/components/ui/slider"
```

```tsx
<Slider defaultValue={[33]} max={100} step={1} />
```

## Range

값 두 개짜리 배열을 주면 구간 슬라이더가 됩니다.

<ComponentPreview styleName="base-nova" name="slider-range" />

## Multiple Thumbs

값을 여러 개 주면 손잡이도 여러 개가 됩니다.

<ComponentPreview styleName="base-nova" name="slider-multiple" />

## Vertical

세로 슬라이더에는 `orientation="vertical"`을 씁니다.

<ComponentPreview styleName="base-nova" name="slider-vertical" />

## Controlled

<ComponentPreview styleName="base-nova" name="slider-controlled" />

## Disabled

`disabled` prop으로 슬라이더를 비활성화합니다.

<ComponentPreview styleName="base-nova" name="slider-disabled" />

## RTL

Dropshot UI에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview styleName="base-nova" name="slider-rtl" direction="rtl" />

## API Reference

자세한 내용은 [Base UI Slider](https://base-ui.com/react/components/slider#api-reference) 문서를 보세요.
