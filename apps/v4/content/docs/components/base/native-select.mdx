---
title: Native Select
description: 브라우저 기본 선택 요소에 스타일만 입혔습니다.
base: base
component: true
---

import { InfoIcon } from "lucide-react"

<Callout variant="info" icon={<InfoIcon className="translate-y-[3px]!" />}>
  스타일이 적용된 셀렉트가 필요하면 [Select](/docs/components/select) 컴포넌트를
  보세요.
</Callout>

<ComponentPreview styleName="base-nova" name="native-select-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add native-select
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="native-select"
  title="components/ui/native-select.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  NativeSelect,
  NativeSelectOptGroup,
  NativeSelectOption,
} from "@/components/ui/native-select"
```

```tsx showLineNumbers
<NativeSelect>
  <NativeSelectOption value="">과일을 고르세요</NativeSelectOption>
  <NativeSelectOption value="apple">Apple</NativeSelectOption>
  <NativeSelectOption value="banana">Banana</NativeSelectOption>
  <NativeSelectOption value="blueberry">Blueberry</NativeSelectOption>
  <NativeSelectOption value="pineapple">Pineapple</NativeSelectOption>
</NativeSelect>
```

## Composition

### Simple

`NativeSelectOptGroup` 없이 `NativeSelect` 바로 아래에 옵션을 두는 구조입니다.

```text
NativeSelect
├── NativeSelectOption
├── NativeSelectOption
├── NativeSelectOption
└── NativeSelectOption
```

### With groups

`NativeSelectOptGroup`으로 옵션을 갈래별로 묶습니다.

```text
NativeSelect
├── NativeSelectOptGroup
│   ├── NativeSelectOption
│   └── NativeSelectOption
└── NativeSelectOptGroup
    ├── NativeSelectOption
    └── NativeSelectOption
```

## Groups

`NativeSelectOptGroup`으로 옵션을 갈래별로 묶습니다.

<ComponentPreview styleName="base-nova" name="native-select-groups" />

## Disabled

`NativeSelect`에 `disabled` prop을 주면 셀렉트가 비활성화됩니다.

<ComponentPreview styleName="base-nova" name="native-select-disabled" />

## Invalid

검증 오류는 `aria-invalid`로 표시하고, 스타일은 `Field` 컴포넌트의 `data-invalid` 속성으로 줍니다.

<ComponentPreview styleName="base-nova" name="native-select-invalid" />

## Native Select vs Select

- 브라우저 기본 동작이 필요하거나, 더 가볍게 만들고 싶거나, 모바일에 맞춘 드롭다운이 필요하면 `NativeSelect`를 씁니다.
- 스타일을 직접 꾸미거나 애니메이션, 복잡한 상호작용이 필요하면 `Select`를 씁니다.

## i18n

언어를 바꾸면 컴포넌트 안의 문구가 함께 바뀝니다. 오른쪽에서 왼쪽으로 읽는 언어를 지원하려면 [RTL 설정 가이드](/docs/rtl)를 보세요.

<ComponentPreview
  styleName="base-nova"
  name="native-select-rtl"
  direction="rtl"
/>

## API Reference

### NativeSelect

네이티브 HTML select 요소를 감싸는 기본 컴포넌트입니다.

```tsx
<NativeSelect>
  <NativeSelectOption value="option1">항목 1</NativeSelectOption>
  <NativeSelectOption value="option2">항목 2</NativeSelectOption>
</NativeSelect>
```

### NativeSelectOption

셀렉트 안의 옵션 하나입니다.

| Prop       | Type      | Default |
| ---------- | --------- | ------- |
| `value`    | `string`  |         |
| `disabled` | `boolean` | `false` |

### NativeSelectOptGroup

관련된 옵션을 묶어 정리합니다.

| Prop       | Type      | Default |
| ---------- | --------- | ------- |
| `label`    | `string`  |         |
| `disabled` | `boolean` | `false` |

```tsx
<NativeSelectOptGroup label="Fruits">
  <NativeSelectOption value="apple">Apple</NativeSelectOption>
  <NativeSelectOption value="banana">Banana</NativeSelectOption>
</NativeSelectOptGroup>
```
