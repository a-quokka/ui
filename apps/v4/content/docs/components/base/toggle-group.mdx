---
title: Toggle Group
description: 켜고 끄는 버튼을 묶어 씁니다.
base: base
component: true
links:
  doc: https://base-ui.com/react/components/toggle-group
  api: https://base-ui.com/react/components/toggle-group#api-reference
---

<ComponentPreview styleName="base-nova" name="toggle-group-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add toggle-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install @base-ui/react
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="toggle-group"
  title="components/ui/toggle-group.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"
```

```tsx
<ToggleGroup type="single">
  <ToggleGroupItem value="a">A</ToggleGroupItem>
  <ToggleGroupItem value="b">B</ToggleGroupItem>
  <ToggleGroupItem value="c">C</ToggleGroupItem>
</ToggleGroup>
```

## Composition

아래 구조로 `ToggleGroup`을 만듭니다.

```text
ToggleGroup
├── ToggleGroupItem
└── ToggleGroupItem
```

## Outline

`variant="outline"`으로 테두리 스타일을 씁니다.

<ComponentPreview styleName="base-nova" name="toggle-group-outline" />

## Size

`size` prop으로 토글 그룹 크기를 바꿉니다.

<ComponentPreview styleName="base-nova" name="toggle-group-sizes" />

## Spacing

`spacing`으로 토글 그룹 항목 사이에 간격을 줍니다.

<ComponentPreview styleName="base-nova" name="toggle-group-spacing" />

## Vertical

세로 토글 그룹에는 `orientation="vertical"`을 씁니다.

<ComponentPreview styleName="base-nova" name="toggle-group-vertical" />

## Disabled

<ComponentPreview styleName="base-nova" name="toggle-group-disabled" />

## Custom

직접 꾸민 토글 그룹 예제입니다.

<ComponentPreview
  styleName="base-nova"
  name="toggle-group-font-weight-selector"
  previewClassName="*:data-[slot=field]:max-w-xs"
/>

## RTL

shadcn/ui에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview
  styleName="base-nova"
  name="toggle-group-rtl"
  direction="rtl"
/>

## API Reference

자세한 내용은 [Base UI Toggle Group](https://base-ui.com/react/components/toggle-group#api-reference) 문서를 보세요.

## Changelog

### 2026-05-17 Default Spacing

기본 `spacing`을 `0`에서 `2`로 바꿔, 이제 토글 그룹 항목 사이에 간격이 생깁니다. 붙여 놓으려면 `spacing={0}`을 쓰세요.
