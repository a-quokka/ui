---
title: Breadcrumb
description: 현재 위치까지의 경로를 링크로 보여 줍니다.
base: base
component: true
---

<ComponentPreview
  styleName="base-nova"
  name="breadcrumb-demo"
  previewClassName="p-2"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add breadcrumb
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="breadcrumb"
  title="components/ui/breadcrumb.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  Breadcrumb,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbList,
  BreadcrumbPage,
  BreadcrumbSeparator,
} from "@/components/ui/breadcrumb"
```

```tsx showLineNumbers
<Breadcrumb>
  <BreadcrumbList>
    <BreadcrumbItem>
      <BreadcrumbLink render={<a href="/" />}>Home</BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbLink render={<a href="/components" />}>
        Components
      </BreadcrumbLink>
    </BreadcrumbItem>
    <BreadcrumbSeparator />
    <BreadcrumbItem>
      <BreadcrumbPage>Breadcrumb</BreadcrumbPage>
    </BreadcrumbItem>
  </BreadcrumbList>
</Breadcrumb>
```

## Composition

아래 구조로 `Breadcrumb`을 만듭니다.

```text
Breadcrumb
└── BreadcrumbList
    ├── BreadcrumbItem
    │   └── BreadcrumbLink
    ├── BreadcrumbSeparator
    ├── BreadcrumbItem
    │   └── BreadcrumbLink
    ├── BreadcrumbSeparator
    └── BreadcrumbItem
        └── BreadcrumbPage
```

## Basic

홈 링크와 컴포넌트 링크가 있는 기본 breadcrumb 입니다.

<ComponentPreview styleName="base-nova" name="breadcrumb-basic" />

## Custom separator

`<BreadcrumbSeparator />`의 `children`으로 원하는 컴포넌트를 넣어 구분 기호를 바꿉니다.

<ComponentPreview styleName="base-nova" name="breadcrumb-separator" />

## Dropdown

`<BreadcrumbItem />`과 `<DropdownMenu />`를 조합하면 breadcrumb 안에 드롭다운을 넣을 수 있습니다.

<ComponentPreview styleName="base-nova" name="breadcrumb-dropdown" />

## Collapsed

breadcrumb이 너무 길 때는 `<BreadcrumbEllipsis />`로 접힌 상태를 보여 줍니다.

<ComponentPreview
  styleName="base-nova"
  name="breadcrumb-ellipsis"
  previewClassName="p-2"
/>

## Link component

라우팅 라이브러리의 링크 컴포넌트를 쓰려면 `<BreadcrumbLink />`의 `render` prop을 씁니다.

<ComponentPreview styleName="base-nova" name="breadcrumb-link" />

## RTL

shadcn/ui에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview
  styleName="base-nova"
  name="breadcrumb-rtl"
  direction="rtl"
  previewClassName="p-2"
/>

## API Reference

### Breadcrumb

`Breadcrumb`은 breadcrumb 전체를 감싸는 루트 내비게이션 요소입니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### BreadcrumbList

`BreadcrumbList`는 breadcrumb 항목을 순서 있는 목록으로 보여 줍니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### BreadcrumbItem

`BreadcrumbItem`은 breadcrumb 항목 하나를 감쌉니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### BreadcrumbLink

`BreadcrumbLink`는 breadcrumb 안의 누를 수 있는 링크입니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### BreadcrumbPage

`BreadcrumbPage`는 breadcrumb에서 현재 페이지를 보여 줍니다. 누를 수 없습니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` | -       |

### BreadcrumbSeparator

`BreadcrumbSeparator`는 breadcrumb 항목 사이의 구분 기호입니다. children을 넘기면 기본 아이콘을 대신할 수 있습니다.

| Prop        | Type              | Default |
| ----------- | ----------------- | ------- |
| `children`  | `React.ReactNode` | -       |
| `className` | `string`          | -       |

### BreadcrumbEllipsis

`BreadcrumbEllipsis`는 접힌 breadcrumb 항목을 말줄임으로 표시합니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` | -       |
