---
title: Input
description: 한 줄 텍스트를 입력받습니다.
base: base
component: true
---

<ComponentPreview
  styleName="base-nova"
  name="input-demo"
  previewClassName="*:max-w-xs"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add input
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="input"
  title="components/ui/input.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { Input } from "@/components/ui/input"
```

```tsx
<Input />
```

## Basic

<ComponentPreview
  styleName="base-nova"
  name="input-basic"
  previewClassName="*:max-w-xs"
/>

## Field

`Field`·`FieldLabel`·`FieldDescription`으로 라벨과 설명이 붙은 입력을 만듭니다.

<ComponentPreview
  styleName="base-nova"
  name="input-field"
  previewClassName="*:max-w-xs"
/>

## Field Group

`FieldGroup`으로 `Field`를 여러 개 늘어놓아 폼을 만듭니다.

<ComponentPreview
  styleName="base-nova"
  name="input-fieldgroup"
  previewClassName="*:max-w-xs"
/>

## Disabled

`disabled` prop으로 입력을 비활성화합니다. 비활성 상태를 꾸미려면 `Field` 컴포넌트에 `data-disabled` 속성을 붙이세요.

<ComponentPreview
  styleName="base-nova"
  name="input-disabled"
  previewClassName="*:max-w-xs"
/>

## Invalid

`aria-invalid` prop으로 입력을 오류 상태로 표시합니다. 오류 상태를 꾸미려면 `Field` 컴포넌트에 `data-invalid` 속성을 붙이세요.

<ComponentPreview
  styleName="base-nova"
  name="input-invalid"
  previewClassName="*:max-w-xs"
/>

## File

`type="file"` prop으로 파일 입력을 만듭니다.

<ComponentPreview
  styleName="base-nova"
  name="input-file"
  previewClassName="*:max-w-xs"
/>

## Inline

`Field`에 `orientation="horizontal"`을 주면 한 줄짜리 입력이 됩니다. `Button`과 함께 두면 버튼이 붙은 검색 입력이 됩니다.

<ComponentPreview
  styleName="base-nova"
  name="input-inline"
  previewClassName="*:max-w-xs"
/>

## Grid

그리드 배치로 입력 여러 개를 나란히 놓습니다.

<ComponentPreview
  styleName="base-nova"
  name="input-grid"
  previewClassName="p-6"
/>

## Required

`required` 속성으로 필수 입력을 표시합니다.

<ComponentPreview
  styleName="base-nova"
  name="input-required"
  previewClassName="*:max-w-xs"
/>

## Badge

라벨에 `Badge`를 넣어 권장 입력을 강조합니다.

<ComponentPreview
  styleName="base-nova"
  name="input-badge"
  previewClassName="*:max-w-xs"
/>

## Input Group

입력 안에 아이콘·글자·버튼을 넣으려면 `InputGroup` 컴포넌트를 씁니다. 예시는 [Input Group](/docs/components/input-group)을 보세요.

<ComponentPreview
  styleName="base-nova"
  name="input-input-group"
  previewClassName="*:max-w-xs"
/>

## Button Group

입력에 버튼을 붙이려면 `ButtonGroup` 컴포넌트를 씁니다. 예시는 [Button Group](/docs/components/button-group)을 보세요.

<ComponentPreview
  styleName="base-nova"
  name="input-button-group"
  previewClassName="*:max-w-xs"
/>

## Form

입력 여러 개와 셀렉트, 버튼이 들어간 전체 폼 예제입니다.

<ComponentPreview
  styleName="base-nova"
  name="input-form"
  previewClassName="h-[32rem]"
/>

## RTL

shadcn/ui에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview
  styleName="base-nova"
  name="input-rtl"
  direction="rtl"
  previewClassName="*:max-w-xs"
/>
