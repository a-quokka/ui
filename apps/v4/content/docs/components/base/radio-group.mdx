---
title: Radio Group
description: 여러 후보 중 하나만 고릅니다.
base: base
component: true
links:
  doc: https://base-ui.com/react/components/radio-group
  api: https://base-ui.com/react/components/radio-group#api-reference
---

<ComponentPreview styleName="base-nova" name="radio-group-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add radio-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install @base-ui/react
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="radio-group"
  title="components/ui/radio-group.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import { Label } from "@/components/ui/label"
import { RadioGroup, RadioGroupItem } from "@/components/ui/radio-group"
```

```tsx showLineNumbers
<RadioGroup defaultValue="option-one">
  <div className="flex items-center gap-3">
    <RadioGroupItem value="option-one" id="option-one" />
    <Label htmlFor="option-one">첫 번째 선택</Label>
  </div>
  <div className="flex items-center gap-3">
    <RadioGroupItem value="option-two" id="option-two" />
    <Label htmlFor="option-two">두 번째 선택</Label>
  </div>
</RadioGroup>
```

## Composition

아래 구조로 `RadioGroup`을 만듭니다.

```text
RadioGroup
├── RadioGroupItem
└── RadioGroupItem
```

## Description

`Field` 컴포넌트로 설명이 붙은 라디오 그룹 항목을 만듭니다.

<ComponentPreview styleName="base-nova" name="radio-group-description" />

## Choice Card

`FieldLabel`로 `Field` 전체를 감싸면 카드처럼 눌러서 고르는 선택지가 됩니다.

<ComponentPreview styleName="base-nova" name="radio-group-choice-card" />

## Fieldset

`FieldSet`과 `FieldLegend`로 라디오 항목을 묶고 라벨과 설명을 붙입니다.

<ComponentPreview styleName="base-nova" name="radio-group-fieldset" />

## Disabled

`RadioGroup`에 `disabled` prop을 주면 모든 항목이 비활성화됩니다.

<ComponentPreview styleName="base-nova" name="radio-group-disabled" />

## Invalid

검증 오류는 `RadioGroupItem`의 `aria-invalid`와 `Field`의 `data-invalid`로 표시합니다.

<ComponentPreview styleName="base-nova" name="radio-group-invalid" />

## RTL

Dropshot UI에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview
  styleName="base-nova"
  name="radio-group-rtl"
  direction="rtl"
/>

## API Reference

자세한 내용은 [Base UI Radio Group](https://base-ui.com/react/components/radio-group#api-reference) 문서를 보세요.
