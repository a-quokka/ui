---
title: Dialog
description: 화면 위에 겹쳐 뜨는 창입니다. 열려 있는 동안 뒤쪽 내용은 조작할 수 없습니다.
featured: true
base: base
component: true
links:
  doc: https://base-ui.com/react/components/dialog
  api: https://base-ui.com/react/components/dialog#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="dialog-demo"
  description="A dialog for editing profile details."
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add dialog
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install @base-ui/react
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="dialog"
  title="components/ui/dialog.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"
```

```tsx showLineNumbers
<Dialog>
  <DialogTrigger>Open</DialogTrigger>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Are you absolutely sure?</DialogTitle>
      <DialogDescription>
        This action cannot be undone. This will permanently delete your account
        and remove your data from our servers.
      </DialogDescription>
    </DialogHeader>
  </DialogContent>
</Dialog>
```

## Composition

아래 구조로 `Dialog`를 만듭니다.

```text
Dialog
├── DialogTrigger
└── DialogContent
    ├── DialogHeader
    │   ├── DialogTitle
    │   └── DialogDescription
    └── DialogFooter
```

## Custom Close Button

기본 닫기 버튼을 직접 만든 버튼으로 바꿉니다.

<ComponentPreview styleName="base-nova" name="dialog-close-button" />

## No Close Button

`showCloseButton={false}`로 닫기 버튼을 숨깁니다.

<ComponentPreview styleName="base-nova" name="dialog-no-close-button" />

## Sticky Footer

내용이 스크롤되는 동안에도 동작 버튼을 계속 보여 줍니다.

<ComponentPreview styleName="base-nova" name="dialog-sticky-footer" />

## Scrollable Content

내용이 길면 헤더는 그대로 두고 본문만 스크롤됩니다.

<ComponentPreview styleName="base-nova" name="dialog-scrollable-content" />

## RTL

Dropshot UI에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview styleName="base-nova" name="dialog-rtl" direction="rtl" />

## API Reference

자세한 내용은 [Base UI](https://base-ui.com/react/components/dialog#api-reference) 문서를 보세요.
