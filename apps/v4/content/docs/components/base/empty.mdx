---
title: Empty
description: 보여 줄 내용이 없을 때의 화면입니다.
base: base
component: true
---

<ComponentPreview
  styleName="base-nova"
  name="empty-demo"
  previewClassName="h-96 p-0"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add empty
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="empty"
  title="components/ui/empty.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import {
  Empty,
  EmptyContent,
  EmptyDescription,
  EmptyHeader,
  EmptyMedia,
  EmptyTitle,
} from "@/components/ui/empty"
```

```tsx
<Empty>
  <EmptyHeader>
    <EmptyMedia variant="icon">
      <Icon />
    </EmptyMedia>
    <EmptyTitle>No data</EmptyTitle>
    <EmptyDescription>No data found</EmptyDescription>
  </EmptyHeader>
  <EmptyContent>
    <Button>Add data</Button>
  </EmptyContent>
</Empty>
```

## Composition

아래 구조로 `Empty` 상태를 만듭니다.

```text
Empty
├── EmptyHeader
│   ├── EmptyMedia
│   ├── EmptyTitle
│   └── EmptyDescription
└── EmptyContent
```

## Outline

`border` 유틸리티 클래스로 테두리가 있는 빈 상태를 만듭니다.

<ComponentPreview
  styleName="base-nova"
  name="empty-outline"
  previewClassName="h-96 p-6 md:p-10"
/>

## Background

`bg-*`와 `bg-gradient-*` 유틸리티로 빈 상태에 배경을 넣습니다.

<ComponentPreview
  styleName="base-nova"
  name="empty-background"
  previewClassName="h-96 p-0"
/>

## Avatar

`EmptyMedia`로 빈 상태에 아바타를 보여 줍니다.

<ComponentPreview
  styleName="base-nova"
  name="empty-avatar"
  previewClassName="h-96 p-0"
/>

## Avatar Group

`EmptyMedia`로 빈 상태에 아바타 그룹을 보여 줍니다.

<ComponentPreview
  styleName="base-nova"
  name="empty-avatar-group"
  previewClassName="h-96 p-0"
/>

## InputGroup

`EmptyContent` 안에 `InputGroup` 컴포넌트를 넣을 수 있습니다.

<ComponentPreview
  styleName="base-nova"
  name="empty-input-group"
  previewClassName="h-96 p-0"
/>

## RTL

Dropshot UI에서 RTL을 켜려면 [RTL 설정 가이드](/docs/rtl)를 확인하세요.

<ComponentPreview styleName="base-nova" name="empty-rtl" direction="rtl" />

## API Reference

### Empty

빈 상태의 기본 컴포넌트입니다. `EmptyHeader`와 `EmptyContent`를 감쌉니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<Empty>
  <EmptyHeader />
  <EmptyContent />
</Empty>
```

### EmptyHeader

`EmptyHeader`는 미디어·제목·설명을 감쌉니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyHeader>
  <EmptyMedia />
  <EmptyTitle />
  <EmptyDescription />
</EmptyHeader>
```

### EmptyMedia

`EmptyMedia`로 빈 상태의 아이콘이나 이미지를 보여 줍니다. 아바타 같은 다른 컴포넌트를 넣을 수도 있습니다.

| Prop        | Type                  | Default   |
| ----------- | --------------------- | --------- |
| `variant`   | `"default" \| "icon"` | `default` |
| `className` | `string`              |           |

```tsx
<EmptyMedia variant="icon">
  <Icon />
</EmptyMedia>
```

```tsx
<EmptyMedia>
  <Avatar>
    <AvatarImage src="..." />
    <AvatarFallback>CN</AvatarFallback>
  </Avatar>
</EmptyMedia>
```

### EmptyTitle

`EmptyTitle`로 빈 상태의 제목을 보여 줍니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyTitle>No data</EmptyTitle>
```

### EmptyDescription

`EmptyDescription`으로 빈 상태의 설명을 보여 줍니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyDescription>You do not have any notifications.</EmptyDescription>
```

### EmptyContent

`EmptyContent`로 버튼·입력·링크처럼 빈 상태의 내용을 보여 줍니다.

| Prop        | Type     | Default |
| ----------- | -------- | ------- |
| `className` | `string` |         |

```tsx
<EmptyContent>
  <Button>Add Project</Button>
</EmptyContent>
```
