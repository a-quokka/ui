---
title: Context Menu
description: 오른쪽 클릭으로 동작 목록을 엽니다.
base: base
component: true
links:
  doc: https://base-ui.com/react/components/context-menu
  api: https://base-ui.com/react/components/context-menu#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="context-menu-demo"
  description="A context menu with sub menu items."
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add context-menu
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install @base-ui/react
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="context-menu"
  title="components/ui/context-menu.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu"
```

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>여기서 오른쪽 클릭</ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## Composition

아래 구조로 `ContextMenu`를 만듭니다.

```text
ContextMenu
├── ContextMenuTrigger
└── ContextMenuContent
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuItem
    │   └── ContextMenuItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuCheckboxItem
    │   └── ContextMenuCheckboxItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   └── ContextMenuRadioGroup
    │       ├── ContextMenuRadioItem
    │       └── ContextMenuRadioItem
    └── ContextMenuSub
        ├── ContextMenuSubTrigger
        └── ContextMenuSubContent
            └── ContextMenuGroup
                ├── ContextMenuItem
                └── ContextMenuItem
```

## Basic

동작 몇 개가 들어간 기본 컨텍스트 메뉴입니다.

<ComponentPreview styleName="base-nova" name="context-menu-basic" />

## Submenu

보조 동작은 `ContextMenuSub`로 중첩합니다.

<ComponentPreview styleName="base-nova" name="context-menu-submenu" />

## Shortcuts

`ContextMenuShortcut`으로 단축키를 표시합니다.

<ComponentPreview styleName="base-nova" name="context-menu-shortcuts" />

## Groups

관련된 동작을 묶고 구분선으로 나눕니다.

<ComponentPreview styleName="base-nova" name="context-menu-groups" />

## Icons

아이콘과 라벨을 함께 두면 한눈에 들어옵니다.

<ComponentPreview styleName="base-nova" name="context-menu-icons" />

## Checkboxes

켜고 끄는 항목에는 `ContextMenuCheckboxItem`을 씁니다.

<ComponentPreview styleName="base-nova" name="context-menu-checkboxes" />

## Radio

하나만 고르는 선택에는 `ContextMenuRadioItem`을 씁니다.

<ComponentPreview styleName="base-nova" name="context-menu-radio" />

## Destructive

`variant="destructive"`로 메뉴 항목을 경고 스타일로 만듭니다.

<ComponentPreview styleName="base-nova" name="context-menu-destructive" />

## Sides

서브메뉴 위치는 side와 align prop으로 정합니다.

<ComponentPreview styleName="base-nova" name="context-menu-sides" />

## i18n

언어를 바꾸면 컴포넌트 안의 문구가 함께 바뀝니다. 오른쪽에서 왼쪽으로 읽는 언어를 지원하려면 [RTL 설정 가이드](/docs/rtl)를 보세요.

<ComponentPreview
  styleName="base-nova"
  name="context-menu-rtl"
  direction="rtl"
/>

`side="inline-end"`를 주면 메뉴가 트리거의 논리적 오른쪽에 놓입니다.

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>여기서 오른쪽 클릭</ContextMenuTrigger>
  <ContextMenuContent side="inline-end">
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## API Reference

자세한 내용은 [Base UI](https://base-ui.com/react/components/context-menu#api-reference) 문서를 보세요.
