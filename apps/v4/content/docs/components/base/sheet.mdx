---
title: Sheet
description: 화면 가장자리에서 밀려 나오는 패널입니다.
base: base
component: true
links:
  doc: https://base-ui.com/react/components/dialog
  api: https://base-ui.com/react/components/dialog#api-reference
---

<ComponentPreview styleName="base-nova" name="sheet-demo" />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add sheet
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>아래 의존성을 설치합니다.</Step>

```bash
npm install @base-ui/react
```

<Step>아래 코드를 프로젝트에 붙여 넣습니다.</Step>

<ComponentSource
  name="sheet"
  title="components/ui/sheet.tsx"
  styleName="base-nova"
/>

<Step>import 경로를 프로젝트 구조에 맞게 고칩니다.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx showLineNumbers
import {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
```

```tsx showLineNumbers
<Sheet>
  <SheetTrigger>Open</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>정말 진행할까요?</SheetTitle>
      <SheetDescription>이 작업은 되돌릴 수 없습니다.</SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```

## Composition

아래 구조로 `Sheet`를 만듭니다.

```text
Sheet
├── SheetTrigger
└── SheetContent
    ├── SheetHeader
    │   ├── SheetTitle
    │   └── SheetDescription
    └── SheetFooter
```

## Side

`SheetContent`의 `side` prop으로 시트가 나타날 화면 가장자리를 정합니다. `top`·`right`·`bottom`·`left` 중에서 고릅니다.

<ComponentPreview styleName="base-nova" name="sheet-side" />

## No Close Button

`SheetContent`에 `showCloseButton={false}`를 주면 닫기 버튼이 숨겨집니다.

<ComponentPreview styleName="base-nova" name="sheet-no-close-button" />

## i18n

언어를 바꾸면 컴포넌트 안의 문구가 함께 바뀝니다. 오른쪽에서 왼쪽으로 읽는 언어를 지원하려면 [RTL 설정 가이드](/docs/rtl)를 보세요.

<ComponentPreview styleName="base-nova" name="sheet-rtl" direction="rtl" />

## API Reference

자세한 내용은 [Base UI Dialog](https://base-ui.com/react/components/dialog#api-reference) 문서를 보세요.
