---
title: Next.js
description: RTL 이 설정된 Next.js 프로젝트를 새로 만듭니다.
---

<Steps>

### Create Project

`--rtl` 플래그와 `next` 템플릿으로 새 프로젝트를 만듭니다.

```bash
npx shadcn@latest create --template next --rtl
```

그러면 `rtl: true` 가 설정된 `components.json` 이 만들어집니다.

```json title="components.json" showLineNumbers {4}
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "base-nova",
  "rtl": true
}
```

### Add DirectionProvider

애플리케이션을 `direction="rtl"` 을 준 `DirectionProvider` 로 감쌉니다.

그다음 `html` 태그에 `dir="rtl"` 과 `lang="ar"` 을 붙입니다. `lang="ar"` 은 대상 언어로 바꾸세요.

```tsx title="app/layout.tsx" showLineNumbers {1,9-13}
import { DirectionProvider } from "@/components/ui/direction"

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ar" dir="rtl">
      <body>
        <DirectionProvider direction="rtl">{children}</DirectionProvider>
      </body>
    </html>
  )
}
```

### Add Font

RTL 을 제대로 쓰려면 대상 언어를 잘 지원하는 폰트를 권합니다. [Noto](https://fonts.google.com/noto) 가 이런 용도에 좋고 Inter·Geist 와도 잘 어울립니다.

```tsx title="app/layout.tsx" showLineNumbers {1,5-8,16}
import { Noto_Sans_Arabic } from "next/font/google"

import { DirectionProvider } from "@/components/ui/direction"

const fontSans = Noto_Sans_Arabic({
  subsets: ["arabic"],
  variable: "--font-sans",
})

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="ar" dir="rtl" className={fontSans.variable}>
      <body>
        <DirectionProvider direction="rtl">{children}</DirectionProvider>
      </body>
    </html>
  )
}
```

히브리어처럼 다른 언어라면 `Noto_Sans_Hebrew` 폰트를 쓰면 됩니다.

### Add Components

이제 컴포넌트를 추가할 준비가 됐습니다. RTL 지원은 CLI 가 알아서 처리합니다.

```bash
npx shadcn@latest add item
```

</Steps>
