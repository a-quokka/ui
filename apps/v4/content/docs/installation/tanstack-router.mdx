---
title: TanStack Router
description: TanStack Router 에 Dropshot UI 를 설치하고 설정합니다.
---

<Steps>

### Create project

새 TanStack Router 프로젝트를 만드는 것부터 시작합니다.

```bash
npx create-tsrouter-app@latest my-app --template file-router --tailwind --add-ons shadcn
```

### Add Components

이제 프로젝트에 컴포넌트를 추가할 수 있습니다.

```bash
npx shadcn@latest add button
```

위 명령으로 `Button` 컴포넌트가 프로젝트에 추가됩니다. 이렇게 가져다 쓰면 됩니다.

```tsx title="src/routes/index.tsx" showLineNumbers {3,12}
import { createFileRoute } from "@tanstack/react-router"

import { Button } from "@/components/ui/button"

export const Route = createFileRoute("/")({
  component: App,
})

function App() {
  return (
    <div>
      <Button>눌러 보세요</Button>
    </div>
  )
}
```

</Steps>
