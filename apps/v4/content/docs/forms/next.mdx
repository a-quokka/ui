---
title: Next.js
description: useActionState 와 Server Actions 로 React 폼을 만듭니다.
---

import { InfoIcon } from "lucide-react"

Next.js 에서 `useActionState` 와 Server Actions 로 폼을 만드는 방법을 살펴봅니다. 폼 만들기, 검증, 대기 상태, 접근성까지 다룹니다.

## Demo

텍스트 입력 하나와 textarea 하나가 있는 폼을 만듭니다. 제출하면 서버 액션으로 데이터를 검증하고 폼 상태를 갱신합니다.

<ComponentPreview
  styleName="new-york-v4"
  name="form-next-demo"
  className="[&_.preview]:h-[700px] [&_pre]:h-[700px]!"
/>

<Callout icon={<InfoIcon />}>
  **참고:** 서버 액션에서 스키마 검증과 오류 처리가 어떻게 동작하는지 보여
  주려고 이 페이지의 예시는 브라우저 기본 검증을 일부러 껐습니다.
</Callout>

## Approach

이 폼은 Next.js 와 React 가 기본으로 제공하는 폼 기능을 씁니다. `<Field />` 로 만들기 때문에 **마크업과 스타일을 온전히 마음대로** 할 수 있습니다.

- 이동과 점진적 향상을 위해 Next.js 의 `<Form />` 을 씁니다.
- 접근성 있는 폼은 `<Field />` 로 만듭니다.
- 폼 상태와 오류는 `useActionState` 로 관리합니다.
- 대기 상태는 pending prop 으로 다룹니다.
- 제출은 Server Actions 로 처리합니다.
- 서버 검증은 Zod 로 합니다.

## Anatomy

`<Field />` 로 만든 기본 폼 예시입니다.

```tsx showLineNumbers
<Form action={formAction}>
  <FieldGroup>
    <Field data-invalid={!!formState.errors?.title?.length}>
      <FieldLabel htmlFor="title">Bug Title</FieldLabel>
      <Input
        id="title"
        name="title"
        defaultValue={formState.values.title}
        disabled={pending}
        aria-invalid={!!formState.errors?.title?.length}
        placeholder="Login button not working on mobile"
        autoComplete="off"
      />
      <FieldDescription>
        Provide a concise title for your bug report.
      </FieldDescription>
      {formState.errors?.title && (
        <FieldError>{formState.errors.title[0]}</FieldError>
      )}
    </Field>
  </FieldGroup>
  <Button type="submit">Submit</Button>
</Form>
```

## Usage

### Create a form schema

먼저 `schema.ts` 에 Zod 스키마로 폼의 모양을 정의합니다.

<Callout icon={<InfoIcon />}>
  **참고:** 이 예시는 `zod v3` 로 검증하지만 다른 검증 라이브러리로 바꿔도
  됩니다. 다만 Standard Schema 규격을 따르는 것이어야 합니다.
</Callout>

```tsx showLineNumbers title="schema.ts"
import { z } from "zod"

export const formSchema = z.object({
  title: z
    .string()
    .min(5, "Bug title must be at least 5 characters.")
    .max(32, "Bug title must be at most 32 characters."),
  description: z
    .string()
    .min(20, "Description must be at least 20 characters.")
    .max(100, "Description must be at most 100 characters."),
})
```

### Define the form state type

다음으로 값·오류·성공 여부를 담는 폼 상태 타입을 만듭니다. 클라이언트와 서버 양쪽에서 폼 상태의 타입으로 씁니다.

```tsx showLineNumbers title="schema.ts"
import { z } from "zod"

export type FormState = {
  values?: z.infer<typeof formSchema>
  errors: null | Partial<Record<keyof z.infer<typeof formSchema>, string[]>>
  success: boolean
}
```

**중요:** 스키마와 `FormState` 타입은 별도 파일에 둡니다. 그래야 클라이언트 컴포넌트와 서버 컴포넌트 양쪽에서 가져다 쓸 수 있습니다.

### Create the Server Action

서버 액션은 서버에서 돌면서 클라이언트가 부를 수 있는 함수입니다. 폼 데이터를 검증하고 폼 상태를 갱신하는 데 씁니다.

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-demo-action.ts"
  title="actions.ts"
/>

**참고:** 오류일 때는 `values` 를 함께 돌려줍니다. 사용자가 입력한 값을 폼 상태에 남겨 두기 위해서입니다. 성공일 때는 빈 값을 돌려줘 폼을 비웁니다.

### Build the form

이제 `<Field />` 로 폼을 만듭니다. `useActionState` 훅으로 폼 상태와 서버 액션, 대기 상태를 관리합니다.

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-demo.tsx"
  title="form.tsx"
/>

### Done

여기까지입니다. 이제 클라이언트와 서버 양쪽 검증이 붙은, 접근성을 갖춘 폼이 생겼습니다.

폼을 제출하면 서버에서 `formAction` 이 불립니다. 서버 액션이 데이터를 검증하고 폼 상태를 갱신합니다.

데이터가 올바르지 않으면 서버 액션이 오류를 클라이언트로 돌려줍니다. 올바르면 성공 상태를 돌려주고 폼 상태를 갱신합니다.

## Pending States

`useActionState` 의 `pending` 으로 로딩 표시를 보여 주고 입력을 잠급니다.

```tsx showLineNumbers {11,26-34}
"use client"

import * as React from "react"
import Form from "next/form"

import { Spinner } from "@/components/ui/spinner"

import { bugReportFormAction } from "./actions"

export function BugReportForm() {
  const [formState, formAction, pending] = React.useActionState(
    bugReportFormAction,
    {
      errors: null,
      success: false,
    }
  )

  return (
    <Form action={formAction}>
      <FieldGroup>
        <Field data-disabled={pending}>
          <FieldLabel htmlFor="name">Name</FieldLabel>
          <Input id="name" name="name" disabled={pending} />
        </Field>
        <Field>
          <Button type="submit" disabled={pending}>
            {pending && <Spinner />} Submit
          </Button>
        </Field>
      </FieldGroup>
    </Form>
  )
}
```

## Disabled States

### Submit Button

제출 버튼을 잠그려면 버튼의 `disabled` 에 `pending` 을 넘깁니다.

```tsx showLineNumbers
<Button type="submit" disabled={pending}>
  {pending && <Spinner />} Submit
</Button>
```

### Field

`<Field />` 에 비활성 상태와 스타일을 주려면 `<Field />` 에 `data-disabled` 를 붙입니다.

```tsx showLineNumbers
<Field data-disabled={pending}>
  <FieldLabel htmlFor="name">Name</FieldLabel>
  <Input id="name" name="name" disabled={pending} />
</Field>
```

## Validation

### Server-side Validation

서버 액션에서 스키마의 `safeParse()` 로 폼 데이터를 검증합니다.

```tsx showLineNumbers title="actions.ts" {12-20}
"use server"

export async function bugReportFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  const result = formSchema.safeParse(values)

  if (!result.success) {
    return {
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }

  return {
    errors: null,
    success: true,
  }
}
```

### Business Logic Validation

서버 액션에 검증 로직을 더 넣을 수도 있습니다.

검증 오류일 때는 값을 꼭 함께 돌려주세요. 그래야 폼 상태에 사용자 입력이 남습니다.

```tsx showLineNumbers title="actions.ts" {22-35}
"use server"

export async function bugReportFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  const result = formSchema.safeParse(values)

  if (!result.success) {
    return {
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }

  // Check if email already exists in database.
  const existingUser = await db.user.findUnique({
    where: { email: result.data.email },
  })

  if (existingUser) {
    return {
      values,
      success: false,
      errors: {
        email: ["This email is already registered"],
      },
    }
  }

  return {
    errors: null,
    success: true,
  }
}
```

## Displaying Errors

오류는 `<FieldError />` 로 입력 옆에 보여 줍니다. `<Field />` 에 `data-invalid` 를, 입력에 `aria-invalid` 를 꼭 붙이세요.

```tsx showLineNumbers
<Field data-invalid={!!formState.errors?.email?.length}>
  <FieldLabel htmlFor="email">Email</FieldLabel>
  <Input
    id="email"
    name="email"
    type="email"
    aria-invalid={!!formState.errors?.email?.length}
  />
  {formState.errors?.email && (
    <FieldError>{formState.errors.email[0]}</FieldError>
  )}
</Field>
```

## Resetting the Form

서버 액션으로 폼을 제출하면 React 가 폼 상태를 초기값으로 되돌립니다.

### Reset on Success

성공했을 때 폼을 비우려면 서버 액션에서 `values` 를 빼면 됩니다. 그러면 React 가 초기값으로 되돌립니다. React 의 기본 동작입니다.

```tsx showLineNumbers title="actions.ts" {22-26}
export async function demoFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  // Validation.
  if (!result.success) {
    return {
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }

  // Business logic.
  callYourDatabaseOrAPI(values)

  // Omit the values on success to reset the form state.
  return {
    errors: null,
    success: true,
  }
}
```

### Preserve on Validation Errors

실패했을 때 폼이 비워지지 않게 하려면 서버 액션에서 값을 돌려주세요. 그래야 사용자 입력이 남습니다.

```tsx showLineNumbers title="actions.ts" {12-17}
export async function demoFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  // Validation.
  if (!result.success) {
    return {
      // Return the values on validation errors.
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }
}
```

## Complex Forms

입력이 여러 개이고 검증이 붙은 조금 복잡한 폼 예시입니다.

<ComponentPreview
  styleName="new-york-v4"
  name="form-next-complex"
  className="[&_.preview]:h-[1100px] [&_pre]:h-[1100px]!"
  hideCode
/>

### Schema

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-complex-schema.ts"
  title="schema.ts"
/>

### Form

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-complex.tsx"
  title="form.tsx"
/>

### Server Action

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-complex-action.ts"
  title="actions.ts"
/>
